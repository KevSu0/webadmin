# Firebase Configuration
# SECURITY WARNING: Never commit actual credentials to version control
# Replace these with your actual Firebase project credentials
# For production deployment, use secure environment variable management

# Firebase Web App Configuration (Safe to expose in frontend)
VITE_FIREBASE_API_KEY=your_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id

# PRODUCTION DEPLOYMENT NOTES:
# - Use your hosting platform's environment variable system
# - Verify all variables are properly set before deployment
# - Firebase web config keys are safe for client-side exposure
# - Server-side operations use serviceAccountKey.json (keep secure)

# Development
NODE_ENV=development
PORT=3001